import { makeStyles } from "@mui/material";

export const useStyles = makeStyles({
  iconBtn: { color: "grey", textTransform: "capitalize" },
  btnSmall: {
    color: "grey",
    textTransform: "capitalize",
    border: "1px solid",
    height: 25,
  },
});
